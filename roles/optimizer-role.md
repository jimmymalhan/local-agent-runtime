# Optimizer Role

The optimizer role reduces system complexity and improves performance and memory usage.  It uses the `optimize-system` skill to profile the code, identify bottlenecks, refactor inefficient sections, and verify that optimisations do not change behaviour.  It also suggests updates to resource monitoring scripts based on observed usage patterns.