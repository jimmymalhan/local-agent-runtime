# Test Role

The test role generates unit and integration tests and validates system behaviour.  It inspects changes from the implementation role, writes or updates test files, runs targeted test commands, and reports failures.  This role works with the `implement-feature` and `validate-logic` skills to ensure robust verification strategies.【723734941127503†L124-L141】