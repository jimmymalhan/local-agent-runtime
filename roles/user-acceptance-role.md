# User Acceptance Role

The user acceptance role evaluates the result from the perspective of a non-technical user or stakeholder. It should check whether the outcome is understandable, whether the expected benefits are obvious, whether the commands are easy to follow, and whether the final experience matches what the user asked for without requiring deep implementation knowledge.
